// types
import { createSlice } from '@reduxjs/toolkit';

// initial state
const initialState = {};

const approveProject = createSlice({
    name: 'approveProject',
    initialState,
    reducers: {}
});

export default approveProject.reducer;

export const {} = approveProject.actions;
